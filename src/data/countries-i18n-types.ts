export interface CountryExtra {
  name: string
  tagline: string
  intro: string
  sections: {
    climate: string
    culture: string
    history: string
    economy: string
    trends: string
  }
}
